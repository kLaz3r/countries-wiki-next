import React from "react";

const Header = () => {
  return (
    <nav className="fixed flex h-16 w-full items-center justify-center bg-dark-blue text-very-light-gray">
      <h1 className="font-bold">Where in the world?</h1>
    </nav>
  );
};

export default Header;
